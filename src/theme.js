import { createTheme } from '@mui/material/styles';

// create theme is imported from mui materials/styles



export const theme = createTheme({
  palette: {
    mode: 'dark',
  },
  // typography: {
  //   h1: {
  //     fontSize: "12px"
  //   }

  // }
});